export interface TimerProps {
    bestScoreTime: number;
    time: number;
    output: string;
    targetTime: number;
    cycle: number;
    intervalCache: number;
    bestScoreIntervalCache: number;
}